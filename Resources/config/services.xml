<?xml version="1.0" encoding="UTF-8" ?>
<container xmlns="http://symfony.com/schema/dic/services"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://symfony.com/schema/dic/services
        http://symfony.com/schema/dic/services/services-1.0.xsd">

    <parameters>
        <!-- TODO: Add you parameters here -->
        <parameter key="fayez_calculator.bar" />
        <parameter key="fayez_calculator.integer_foo" />
        <parameter key="fayez_calculator.integer_bar" />
    </parameters>
    <services>
        <!-- TODO: Add your services here -->
        <!-- Service -->
        <service id="fayez_calculator.service" class="Fayez\CalculatorBundle\Service\Service">
            <!-- TODO: Add your service constructor parameters here -->
            <argument key="$em" type="service" id="doctrine.orm.entity_manager"/>
            <argument key="$token" type="service" id="security.token_storage"/>
            <argument key="$requestStack" type="service" id="request_stack"/>
            <argument key="$translator" type="service" id="translator"/>
            <argument key="$bar" type="string">%fayez_calculator.bar%</argument>
            <argument key="$integerFoo" type="string">%fayez_calculator.integer_foo%</argument>
            <argument key="$integerBar" type="string">%fayez_calculator.integer_bar%</argument>
        </service>

        <!-- TODO: Add the alias for your services here -->
        <service id="Fayez\CalculatorBundle\Service\Service" alias="fayez_calculator.service" />
        <!-- End Service -->

        <!-- TODO: Add your controllers here -->
        <!-- Controller -->
        <service id="fayez_calculator.controller" class="Fayez\CalculatorBundle\Controller\FayezCalculatorController" public="true">
            <!-- TODO: Add your controller constructor parameters here -->
            <argument key="$service" type="service" id="fayez_calculator.service"/>
        </service>

        <!-- TODO: Add the alias for your controllers here -->
        <service id="Fayez\CalculatorBundle\Controller\FayezCalculatorController" alias="fayez_calculator.controller" public="true"/>
        <!-- End Controller -->
    </services>
</container>